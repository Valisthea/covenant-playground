node_modules
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript incremental build caches
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env
.env
.env.local
.env.*.local

# Vercel
.vercel

# WASM build output is COMMITTED (the playground deploys from git, and
# the Vercel build environment has no Rust toolchain to regenerate it).
# Sprint 22 Phase 5 will replace this manual commit with an automated
# PR opened by the compiler repo's CI on each tag.
#
# Do NOT add public/covenant-wasm/ here — every release of the
# playground bumps the bundle alongside the source.

.vercel
